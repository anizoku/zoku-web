{
  "name": "Post",
  "type": "object",
  "properties": {
    "content": {
      "type": "string",
      "description": "Post text content"
    },
    "image_url": {
      "type": "string",
      "description": "Optional image URL"
    },
    "anime_title": {
      "type": "string",
      "description": "Related anime/manga title"
    },
    "post_type": {
      "type": "string",
      "enum": [
        "discussion",
        "review",
        "reaction",
        "theory",
        "general"
      ],
      "default": "general"
    },
    "community_id": {
      "type": "string",
      "description": "ID of the community this post belongs to (null = general feed)"
    },
    "likes_count": {
      "type": "number",
      "default": 0
    },
    "comments_count": {
      "type": "number",
      "default": 0
    },
    "liked_by": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "author_name": {
      "type": "string"
    },
    "author_avatar": {
      "type": "string"
    },
    "author_level": {
      "type": "number",
      "default": 1,
      "description": "Author's level at time of posting"
    }
  },
  "required": [
    "content"
  ],
  "rls": {
    "create": {
      "created_by": "{{user.email}}"
    },
    "read": {},
    "update": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "data.community_creator_email": "{{user.email}}"
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "data.community_creator_email": "{{user.email}}"
        }
      ]
    }
  }
}